# Spec / Ambient Update

Ambient updates respond to changed vault state without broad reprocessing.

- start from the changed file or attached context
- follow local wikilinks and nearby compiled memory first
- make only narrow, warranted updates
- avoid broad vault scans unless the local context is clearly insufficient

Ambient mode is for opportunistic stewardship, not a full routine run.
